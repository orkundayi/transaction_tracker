name: mutual
version: 0.0.1
publish_to: "none"

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  android_intent_plus: ^5.2.1
  collection: ^1.19.0
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  logging: ^1.3.0
  receive_intent: ^0.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter: